# X-Agent Development Dependencies
# Includes all production dependencies plus development tools
# Python >= 3.11

# Production dependencies
-r requirements.txt

# Testing
pytest==8.2.0
pytest-asyncio==0.25.0   # PINNED: 0.23 has collection bug, 1.x changes async-fixture mechanics (2929 errors)
pytest-cov==4.0.0
aiosqlite==0.20.0
locust==2.20.0
# 内存 Redis 测试替身：RateLimiterRedis / CSRFTokenStoreRedis 等依赖 db_manager.redis
fakeredis==2.35.1
# fakeredis 执行 Redis Lua 脚本（check_rate_limit 的原子限流）所需的 Lua 运行时绑定
lupa==2.5

# CLI runtime imports exercised by tests such as tests/test_lite_mode.py
typer>=0.12.0
rich>=13.0.0
prompt-toolkit>=3.0.0
PyYAML>=6.0

# Code Quality & Linting
ruff==0.5.0
black==24.1.0
isort==5.13.0
flake8==7.0.0
pylint==3.0.0
mypy==1.8.0

# Security Scanning
bandit==1.7.0
pip-audit==2.6.1

# Pre-commit Hooks
pre-commit==3.0.0

# Development Utilities
ipython==8.12.0
nest-asyncio>=1.6.0   # test: nested event loop (test_security.py)
