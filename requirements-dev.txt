# X-Agent Development Dependencies
# Includes all production dependencies plus development tools
# Python >= 3.11

# Production dependencies
-r requirements.txt

# Testing
pytest==9.0.3  # P1-05: PYSEC-2026-1845 fix
pytest-asyncio>=0.26.0   # P1-05: pytest 9 兼容（1.x async-fixture 机制已验证：RC 门禁+定向套件绿）
pytest-cov==4.0.0
pytest-timeout==2.3.1    # provides the @pytest.mark.timeout marker used by several suites
psutil>=6.0.0            # also in requirements.txt; required by performance/skill-sandbox tests
aiosqlite==0.20.0
locust==2.20.0
# 内存 Redis 测试替身：RateLimiterRedis / CSRFTokenStoreRedis 等依赖 db_manager.redis
fakeredis==2.35.1
# fakeredis 执行 Redis Lua 脚本（check_rate_limit 的原子限流）所需的 Lua 运行时绑定
lupa==2.5

# Code Quality & Linting
ruff==0.5.0
black==24.1.0
isort==5.13.0
flake8==7.0.0
pylint==3.0.0
mypy==1.8.0

# Security Scanning
bandit==1.7.0
pip-audit==2.6.1

# Pre-commit Hooks
pre-commit==3.0.0

# Development Utilities
ipython==8.12.0
nest-asyncio>=1.6.0   # test: nested event loop (test_security.py)
