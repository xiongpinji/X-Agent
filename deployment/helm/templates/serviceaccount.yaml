# P1-15: api/worker/beat/canary 引用的 ServiceAccount; 应用当前不调用 K8s API,
# 不绑定任何 Role(最小权限), 未来需要时再补 RBAC
apiVersion: v1
kind: ServiceAccount
metadata:
  name: xagent
  namespace: {{ .Release.Namespace }}
  labels:
    app: xagent
automountServiceAccountToken: false
