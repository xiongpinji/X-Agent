# X-Agent 原创内核计划：项目总览与开发指南

日期：2026-05-12

这份文档的目标不是只记录某一轮整理，而是让新成员或协作者快速明白：**这个项目整体能做什么、各模块怎么协作、应该如何开发、如何继续扩展**。

---

## 1. 项目是什么

X-Agent 原创内核计划是一个面向“AI 代理执行系统”的后端内核项目，核心目标是把以下能力组织成统一平台：

- 任务理解与执行
- 工作流编排与回放
- 桌面 / 浏览器自动化
- 开源工具与依赖发现
- 内存、审计、治理与安全控制
- 面向前端页面的结构化视图模型输出

你可以把它理解成一个“AI 代理操作系统内核”：

- 上层负责接收任务、编排流程、展示状态
- 中层负责调用工具、管理运行、保存证据
- 下层负责真正执行桌面、浏览器、搜索、检索等动作

---

## 2. 整体功能版图

当前项目的功能可以分成几大块：

### 2.1 任务与工作流

负责把复杂任务拆成可执行节点，并管理整个生命周期。

能做的事情包括：

- 定义 workflow
- 执行 workflow run
- 记录每个 node 的状态、输出、错误、重试次数
- 建立 failure / compensation 链
- 输出 timeline、summary、metrics、trace ids
- 为前端提供结构化回放数据

对应模块：

- `backend/app/core/workflows.py`
- `backend/app/api/workflows.py`

---

### 2.2 桌面自动化

负责真实桌面环境中的页面交互、编辑、快捷键、输入法和剪贴板动作。

当前可以做的事情包括：

- 打开页面、切换标签页、刷新、关闭
- 点击、双击、右键、拖拽
- 输入文本、选择文本、清除选区
- 光标移动、整词删除、整行删除、整文档跳转
- 复制、剪切、粘贴、剪贴板读写
- 输入法状态控制、候选词选择、候选序列流转
- 快捷键组合、快捷键序列、宏动作

对应模块：

- `backend/app/services/desktop/ui_tars_client.py`

---

### 2.3 开源与工具发现

负责从多个开源生态、包生态和文档生态里发现候选工具、依赖或参考实现。

当前可以做的事情包括：

- 搜索 GitHub / GitLab / Bitbucket / Gitea / Forgejo / Codeberg / SourceForge / Savannah / Launchpad / SourceHut
- 搜索 PyPI / npm / Maven / crates.io / RubyGems / Go package
- 搜索文档站点和静态候选源
- 计算候选分数
- 输出 shortlist、blocked、candidate
- 记录来源、标签、理由、元数据

对应模块：

- `backend/app/core/open_source.py`

---

### 2.4 记忆、审计与状态治理

负责保存运行上下文、审计锚点、状态快照和后续追踪需要的元数据。

这部分通常包括：

- memory / retrieval
- run snapshot
- trace / audit anchors
- failure chain
- compensation chain
- 状态与证据记录

在项目中，它们通常与 workflow 和执行层一起使用。

---

### 2.5 前端可渲染视图模型

项目并不只输出“纯 JSON 数据”，而是逐渐把一些关键 API 收敛成“前端可直接消费的 UI 结构描述”。

最典型的是 workflow 相关接口，它会返回：

- layout
- slots
- panels
- components
- component map
- empty states
- metrics
- trace summaries

这让前端可以按组件树直接渲染，而不是再自己猜接口语义。

---

## 3. 当前核心模块怎么协作

### 3.1 典型调用链

一次完整执行通常会像这样：

1. 用户提交任务
2. 系统生成或选择 workflow
3. workflow 调度器拆分节点并执行
4. 节点执行时调用桌面自动化或开源发现等工具
5. 运行过程中记录 memory / trace / audit 信息
6. 执行完成后生成回放结构和状态汇总
7. 前端根据结构化视图渲染 timeline、nodes、failures、compensations 等区域

---

### 3.2 各模块分工

- **workflow**：决定任务如何分解、如何推进、如何恢复
- **desktop**：处理具体 UI 操作和真实输入事件
- **open_source**：负责检索候选方案、依赖、工具、参考资料
- **memory / audit**：负责留痕、追踪、复盘
- **API 层**：把内部结构输出成前端可消费的数据

---

## 4. 现在项目整体能做什么

从能力角度看，这个项目当前已经可以覆盖以下场景：

### 4.1 代理式任务执行

- 根据自然语言或上游任务构建执行流程
- 自动执行节点并记录中间状态
- 遇到失败时进入补偿链
- 输出可追溯的回放信息

### 4.2 桌面自动化

- 操作网页或桌面 UI
- 处理复杂文本编辑动作
- 控制输入法和剪贴板
- 执行宏级快捷键动作

### 4.3 工具和依赖选型

- 搜索开源实现
- 搜索包和文档资料
- 为后续功能实现寻找参考
- 建立候选评分和筛选机制

### 4.4 前端展示

- 展示 workflow 状态
- 展示 timeline / nodes / failures / compensations / traces
- 展示结构化组件映射
- 为后续 React / Mantine 页面渲染提供 schema

---

## 5. 怎么开发这个项目

下面是建议的开发方式。

### 5.1 开发原则

#### 先定义语义，再定义实现

比如你要加一个新能力，不要先急着写执行代码，先想清楚：

- 这属于 workflow、desktop 还是 open_source？
- 是原始动作还是序列动作？
- 需要记录什么元数据？
- 需要怎么让前端展示？

#### 先统一数据结构，再扩展动作

这点尤其重要：

- workflow 先统一 view model
- desktop 先统一 action 分发
- open_source 先统一 candidate 模型

否则后面会越来越散。

#### 每加一个能力，都要考虑可追溯性

最好同时补：

- timestamp
- provider
- source
- reason
- trace id / run id
- 额外 metadata

---

### 5.2 推荐开发顺序

如果你要继续迭代，推荐按这个顺序：

#### 第一层：核心语义

- 定义 workflow 的节点类型、状态、补偿逻辑
- 定义 desktop 的动作分类和序列执行规则
- 定义 open_source 的候选打分和去重规则

#### 第二层：执行能力

- workflow 调度器
- desktop action 执行器
- open_source provider 搜索

#### 第三层：证据与展示

- timeline / audit anchors
- summary / metrics
- view model / UI schema

#### 第四层：前端消费

- 让前端根据组件树渲染页面
- 做更细的 Mantine / shadcn/ui / React 对接

---

### 5.3 新增一个能力时怎么做

举例：如果要新增一个桌面动作或一个新的开源 provider，建议按以下步骤：

1. 先补数据模型
2. 再补执行函数或 provider
3. 再补支持动作白名单 / 注册列表
4. 再补序列化或 view model 展示
5. 最后跑 lints 和回归检查

---

## 6. 如何继续扩展

### 6.1 扩展 workflow

可以继续做：

- 更严格的 node 类型定义
- 更完整的 compensation policy
- 更细的 timeline event 结构
- 更丰富的 metrics
- 更清楚的前端 props 映射

### 6.2 扩展 desktop

可以继续做：

- 真正的 macro 执行器
- 候选词队列状态管理
- 剪贴板历史栈
- 输入法候选窗状态
- 更完整的键盘序列 DSL

### 6.3 扩展 open_source

可以继续做：

- provider 去重
- 统一评分函数
- 数据源优先级
- 过滤策略
- 结果聚合与排序

---

## 7. 研发时最重要的约束

### 7.1 不要让结构散掉

当前项目最怕的不是功能少，而是：

- 同一种语义被写成多个版本
- 同一个动作在多个地方重复实现
- 同一个 view model 被混用成数据表和 UI schema

### 7.2 保持层次清晰

- model 层：定义结构
- service 层：执行动作
- api 层：输出给前端
- ui 层：渲染页面

### 7.3 一切都要可追踪

尤其是 workflow 和 desktop：

- 不能只知道“做了什么”
- 还要知道“为什么做”“谁触发的”“哪一轮执行的”

---

## 8. 当前代码里最关键的三个文件

### `backend/app/core/open_source.py`
负责开源候选发现和多源 provider 聚合。

### `backend/app/api/workflows.py`
负责把 workflow 运行结果转成前端可消费的结构化视图。

### `backend/app/services/desktop/ui_tars_client.py`
负责桌面自动化动作执行、序列动作、IME、剪贴板和快捷键处理。

---

## 9. 给新开发者的一句话说明

如果你第一次接触这个项目，可以这样理解：

> 这是一个把“任务编排、桌面执行、开源发现、状态追踪和前端展示”统一在一起的 AI 代理内核。

开发时的核心思路是：

> 先定义能力，再统一数据，再补执行，再补展示，最后再扩展细节。

---

## 10. 后续可以直接用作 README 的内容摘要

如果你要把这份文档再压缩成仓库首页简介，可以写成：

- 本项目是一个 AI 代理执行内核
- 支持 workflow 编排与回放
- 支持桌面自动化与输入法 / 剪贴板 / 快捷键控制
- 支持多源开源工具与依赖发现
- 支持结构化 UI 视图模型输出
- 适合继续向前端页面、自动化平台和代理执行系统演进
