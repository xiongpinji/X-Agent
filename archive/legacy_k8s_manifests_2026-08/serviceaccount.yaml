# P1-15: canary/回滚脚本引用的 ServiceAccount; 原 deployment/kubernetes/ 套引用了它但全仓从未定义
# 当前应用不调用 K8s API, 故不绑定任何 Role(最小权限); 未来需要时再补充 RBAC
apiVersion: v1
kind: ServiceAccount
metadata:
  name: xagent
  namespace: xagent
  labels:
    app: xagent
automountServiceAccountToken: false
