"""Utility module providing a simple addition function."""


def add(a: float, b: float) -> float:
    """Return the sum of two numbers.

    Args:
        a: First addend.
        b: Second addend.

    Returns:
        The sum of ``a`` and ``b``.
    """
    return a + b
