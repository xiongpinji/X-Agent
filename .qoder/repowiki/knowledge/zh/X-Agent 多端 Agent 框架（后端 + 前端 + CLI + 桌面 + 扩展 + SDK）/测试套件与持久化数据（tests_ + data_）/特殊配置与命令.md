根级 `pytest` 会自动创建临时 SQLite 与 fakeredis，无需外部依赖；若需真实 LLM/MCP/Qdrant 则需额外设置 `XAGENT_QDRANT_URL` 等环境变量。性能测试通过 `python -m tests.performance.run_tests --suite all [--output-dir performance_reports]` 或 `pytest -m benchmark|load_test|stress_test|stability_test|bottleneck_analysis` 运行；综合测试可执行 `python tests/run_comprehensive_tests.py`。