pytest + pytest-asyncio + pytest-xdist（xdist 并行时通过 `PYTEST_XDIST_WORKER` 环境变量将各 worker 的数据路径重定向到独立 tempdir，避免 Windows 上共享 `data/*.jsonl` 的 PermissionError）；SQLAlchemy async（`aiosqlite` + `NullPool`）；fakeredis（可选 lupa Lua）；Locust（`performance/locustfile.py`）。