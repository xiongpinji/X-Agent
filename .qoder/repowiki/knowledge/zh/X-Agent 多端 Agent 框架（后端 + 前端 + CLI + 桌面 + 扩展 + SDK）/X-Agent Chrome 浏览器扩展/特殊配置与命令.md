本地开发需先 `npm install` 安装 devDependencies，然后 `npm test` 或 `npm run test:integration` 运行测试；打包发布用 `npm run package` 生成不含 node_modules/tests/scripts/*.md 的 zip；调试需在 Chrome 开发者模式加载本目录，并在系统注册 `native-messaging-host.json.example` 对应的 native host 才能连通 MCP。