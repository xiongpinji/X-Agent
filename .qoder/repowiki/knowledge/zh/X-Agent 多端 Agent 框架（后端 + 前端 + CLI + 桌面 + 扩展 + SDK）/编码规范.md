- 所有 Python 子模块遵循 ruff line-length=100、target-version=py311、black 兼容的 isort profile，禁止 E501（由 black 处理）。
- 环境变量统一使用 `XAGENT_` 前缀（如 `XAGENT_DATABASE_URL`、`XAGENT_REDIS_URL`），由 `backend.app.settings.Settings` 集中读取。
- pytest 用例按功能域划分 marker（unit/integration/contracts/e2e/performance），由 `Makefile` 统一筛选运行而非各自维护脚本。
- 版本声明只存在于 `pyproject.toml` 的 `[project] version`，CHANGELOG/ROADMAP/README/前端/SDK 均引用该字段，禁止分散硬编码。