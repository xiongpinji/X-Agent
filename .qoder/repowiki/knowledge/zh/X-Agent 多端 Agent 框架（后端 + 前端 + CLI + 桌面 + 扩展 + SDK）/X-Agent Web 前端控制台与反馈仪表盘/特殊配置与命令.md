开发环境需设置 `VITE_API_URL` 与 `VITE_WS_URL` 环境变量（默认代理至 `localhost:8000`）；启动命令 `npm run dev` 在 3000 端口运行 Vite dev server，支持 `/api` 与 `/ws` 双端点代理；生产构建执行 `npm run build`（先 `tsc` 再 `vite build`），产出目录 `dist/` 可直接被 Dockerfile 托管。