Vite 6 + @vitejs/plugin-react (Fast Refresh) + TypeScript 5；UI 基于 Tailwind CSS 3 + clsx；状态管理使用 zustand 4 与 @tanstack/react-query 5；图表用 recharts 2，图标用 lucide-react；HTTP 客户端为 axios + 原生 fetch（SSE 走 EventSource）；测试框架 jest，构建产物经 terser/lightningcss 压缩。