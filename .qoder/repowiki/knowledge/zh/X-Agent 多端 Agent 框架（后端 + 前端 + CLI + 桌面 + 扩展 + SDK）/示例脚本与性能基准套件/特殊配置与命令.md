运行前置：在仓库根执行 `pip install -e ".[dev]"`；浏览器示例首次需 `playwright install chromium`；完整基准套件入口为 `python benchmarks/run_benchmarks.py`，也可单独跑 `python -m benchmarks.agent_v2_benchmark` 或 `python -m benchmarks.agent_v2_integration_benchmark`。