生产部署需设置 `XAGENT_APP_MODE=production` 并同时配置强 `XAGENT_JWT_SECRET`/`XAGENT_ENCRYPTION_KEY`/`XAGENT_AUDIT_HMAC_SECRET`，且 `database_url` 指向 Postgres、`memory_backend`/`trace_backend`/`admin_store_backend` 不可为 memory/jsonl/sqlite，否则 Settings 构造阶段直接抛错拒绝启动；开发环境可通过 `.env` 或环境变量覆盖默认 SQLite+内存后端。