开发：`cd desktop && cargo tauri dev`（自动触发 `npm --prefix ../frontend run dev` 启动 Vite 3000 端口并加载 `devPath`）；构建：`cargo tauri build`（先执行 `npm --prefix ../frontend run build` 生成 `../frontend/dist`，再打包为 `msi`/`nsis`）；图标生成：`python scripts/generate_icons.py`。