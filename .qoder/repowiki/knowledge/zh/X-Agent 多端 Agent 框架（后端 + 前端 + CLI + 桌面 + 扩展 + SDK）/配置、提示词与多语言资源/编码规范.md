- 配置文件统一采用 YAML/JSON 键值结构，并通过 `${VAR_NAME}` 语法引用环境变量，避免硬编码凭据。
- 提示词文件遵循统一的 YAML frontmatter 元数据规范（id/name/version/purpose/scope/variables 等），并以语义化版本号管理变更。
- i18n 资源按语言拆分为独立 JSON 文件，键名按 common/navigation/errors/validation/i18n 等命名空间分组，保持跨语言一致。